/**
 * Dev-only. `true` fakes airplane mode: banner, toasts, TanStack offline,
 * and products fetch fails so the first-page cache can show.
 * Set back to `false` when done testing.
 */
export const MOCK_OFFLINE = false
